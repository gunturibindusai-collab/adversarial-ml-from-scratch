[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "advml"
version = "0.1.0"
description = "Adversarial ML implemented from scratch"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
